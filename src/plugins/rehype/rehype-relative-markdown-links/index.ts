// Export types
export type { CollectionConfig, Options } from './options'

// Export the remark plugin as default and named export
export { default, default as remarkRelativeMarkdownLinks } from './plugin'
